//use crate::front_of_house::waitlist; //this represent a path starting from the root of the project
//use self::front_of_house::waitlist; //this represent a path starting from the current directory of the file been executed
//this allows us to bring in items from other modules into scope and also export it from here
//pub use crate::front_of_house::waitlist;
fn main() {
    println!("Hello, world!");
}
